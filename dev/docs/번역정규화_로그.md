# 번역 정규화 로그 — 핵심 기계용어 → 정본 용어집

정본 = `_glossary/Pathfinder2e.md`(사용자 용어집). 도구 = `tools/normalize_terms.mjs`(재실행 가능). 날짜 2026-07-01.
원칙: 레거시 DB(cs_data.js)·FVTT 오버레이·파생 글로서리의 **이름(name) 값**을 용어집으로 통일 →
설명문 참조는 @UUID/traitTag/글로서리가 이 name 값을 해소하므로 참조가 자동 정합.

## 1차 (v0.29) — 조건·특성·생물유형·기술 name 통일: 170건 + 오프-가드 35

| 카테고리 | 건수 | 대상 |
|---|---|---|
| condition / condition-list | 70 / 18 | overlay conditions.ko.json · cs_data CONDITIONS_DATA·CONDITIONS · creatures _glossary |
| trait / trait-db | 53 / 27 | creatures _glossary · derived traits.json · cs_data TRAIT_DB |
| skill | 2 | creatures _glossary |

대표: `off-guard` 무방비(전 표면·설명문 35건 포함) · `drained` 흡수됨→생명력 고갈 · `clumsy` 서투른→둔함 ·
`aberration` 오역 변형→기형체(morph=변형 구분) · `prone` 엎드린→넘어뜨려짐 · `sickened` 병약→구역질.

## 2차 (v0.30) — 행동·피해유형 name + 조건 @UUID 라벨

- **name 17**: action 9(stride 이동→보폭, step→걸음, demoralize→사기 꺾기, feint→기만 동작, shove→밀기, reposition→재배치, hustle→급행군, hide→숨기, raise-a-shield→방패 올리기) · damage_type 5(acid→산성, cold→냉기, force→힘, slashing→참격, spirit→영혼) · rarity 1(uncommon→비일상) · skill 2(intimidation→위협, society→사회).
- **조건 @UUID 라벨 4134건**: `@UUID[...conditionitems.Item.<id>]{라벨}`을 id→slug→정본으로 교체(전 오버레이). 런타임 해소값과 완전 일치. ⚠ 라벨 내부에 `{}`/`[]`(임베드 `[[/act ..]]` 매크로) 있는 복잡라벨 12개는 경계오인 손상 방지 위해 스킵.

## 3차 (v0.31) — 설명문 평문 "X 상태" 조건 참조 499건

`<옛조건어>( <값>)? 상태` 앵커로 안전 치환(가려진→은폐, 서투른→둔함, 병약→구역질, 느려짐→둔화, 엎드린→넘어뜨려짐, 붙잡힌→조이기, 흡수됨→생명력 고갈, 고정된→이동 불가, 혼미→멍청함, 매혹된→매혹 등 15어).
"상태"(=조건) 접미가 게임 조건을 강하게 지시 → 샘플 전수 조건참조(오탐 0). ⚠ 공통명사성 강한 옛어(실명/부서진/투명한)와 값-only 평문(예 "병약 2와")은 제외(오탐 위험 → 정밀 패스).

## 스캔 결과

- 오버레이 name **빈값 0**. **영어만 5**(희귀 AP·PF2e-KR 원본 소스 없음=보강 불가):
  actions `shattered-earth`·`de-animating-gestures-false`·`blend-in` / backgrounds `demon-hunted` / deities `lalaci`.

## 남은 작업(정밀 패스 대상)

- **비-"상태" 평문 조건 참조**(값-only "병약 2", 공통명사 옛어) — 문맥 인식/@UUID화 필요.
- **오역(의미) 감사** · **엔티티 이름 교차 감사**(레거시 DB vs 오버레이).
